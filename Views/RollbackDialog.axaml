<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="SqlVersionControl.Views.RollbackDialog"
        Title="Confirm Rollback"
        Width="500" Height="400"
        WindowStartupLocation="CenterOwner"
        CanResize="False">

    <Grid RowDefinitions="Auto,*,Auto">
        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource ButtonDanger}" Padding="20,15">
            <StackPanel>
                <TextBlock Text="⚠ Rollback Confirmation" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource ButtonForeground}"/>
                <TextBlock Name="WarningText" Foreground="{DynamicResource TextPrimary}" FontSize="12" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Preview -->
        <Grid Grid.Row="1" RowDefinitions="Auto,*">
            <TextBlock Grid.Row="0" Text="This ALTER statement will be executed:"
                       FontWeight="SemiBold" Margin="15,10,15,5"/>
            <Border Grid.Row="1" Background="{DynamicResource EditorBackground}" Margin="15,0,15,10">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <TextBlock Name="PreviewText"
                               FontFamily="Consolas,Menlo,Monaco,monospace"
                               FontSize="11"
                               Foreground="{DynamicResource TextPrimary}"
                               Padding="10"/>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="15">
            <Button Name="CancelButton" Content="Cancel" Classes="btn-secondary" Padding="20,8" Margin="0,0,10,0"/>
            <Button Name="ConfirmButton" Content="Execute Rollback"
                    Classes="btn-danger" Padding="20,8"/>
        </StackPanel>
    </Grid>
</Window>
